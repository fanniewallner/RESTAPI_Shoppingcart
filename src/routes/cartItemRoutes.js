/*const express = require("express");
const router = express.Router();
//importera alla controllers

const {} = require("../controllers/cartController");

module.exports = router;

//Get all cartItems by cartId
//api/v1/cart/:cartItem
router.get("/cart/:cartId", getCartItems);

//Add cartItem to cart
//api/v1/cart/:cartItem
router.post("/cart/:cartItemId", addProductToCart);

//delete product from cart
//api/v1/
router.delete("/cart/:cartItemId", deleteItemFromCart);
*/
